# Buoy Tracker Package

# Single source of truth for the application version (moved out of user
# config in v2.0 — review decision Q6). Deployments never edit config for
# a version bump.
__version__ = "2.1.0"
