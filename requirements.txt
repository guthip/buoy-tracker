# Minimal runtime requirements for running the Flask app and MQTT handler
Flask>=2.3
python-dotenv
requests
cryptography>=42.0.0
gunicorn>=21.0.0
Flask-Limiter>=3.3.0

# Meshtastic mesh network (uses protobufs for data encoding)
meshtastic==2.7.4
paho-mqtt==2.1.0

# Protocol buffer JSON conversion for compatibility
protobuf>=3.20

# Optional: SQLAlchemy (kept as optional runtime DB dependency)
SQLAlchemy>=2.0
